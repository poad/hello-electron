const builder = require('electron-builder');

builder.build({
    config: {
        'appId': 'local.test.hello-electron',
        'win':{
            'target': {
                'target': 'zip',
                'arch': [
                    'x64',
                    'arm64',
                ]
            }
        }
    }
});
